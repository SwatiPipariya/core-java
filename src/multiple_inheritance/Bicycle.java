package multiple_inheritance;

// 	Using interfaces we can achieve multiple inheritance ; we cannot achieve multiple inheritance using classes

interface Bicycle {
	
    void applyBrake(int decrement);
    
    void speedUp(int increment);
	
}

